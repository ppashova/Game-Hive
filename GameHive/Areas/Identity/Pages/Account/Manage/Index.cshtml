@page
@model IndexModel
@{
    ViewData["Title"] = "Profile";
    ViewData["ActivePage"] = ManageNavPages.Index;
}
<link rel="stylesheet" href="~/css/Registration.css"/>
<div class="container py-5">
    <h1 class="page-title">@ViewData["Title"]</h1>
    <p class="subtitle">Manage your account information</p>

    <partial name="_StatusMessage" for="StatusMessage" />

    <div class="row">
        <div class="col-md-3">
            <div class="card mb-4">
                <div class="card-header">
                    <h3 class="mb-0">Navigation</h3>
                </div>
                <div class="card-body">
                    <partial name="_ManageNav" />
                </div>
            </div>
        </div>
        <div class="col-md-9">
            <div class="card">
                <div class="card-header">
                    <h3 class="mb-0">Edit Profile</h3>
                </div>
                <div class="card-body">
                    <form id="profile-form" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

                        <div class="input-group">
                            <label asp-for="Username" class="form-label"></label>
                            <input asp-for="Username" class="form-control input-with-icon" placeholder="Please choose your username." />
                            <i class="fas fa-user input-icon"></i>
                        </div>

                        <div class="input-group">
                            <label asp-for="Input.PhoneNumber" class="form-label"></label>
                            <input asp-for="Input.PhoneNumber" class="form-control input-with-icon" placeholder="Please enter your phone number." />
                            <i class="fas fa-phone input-icon"></i>
                            <span asp-validation-for="Input.PhoneNumber" class="text-danger"></span>
                        </div>

                        <button id="update-profile-button" type="submit" class="btn btn-primary">
                            Save Changes <i class="fas fa-save ms-2"></i>
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}